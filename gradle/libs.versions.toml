# TODO: move everything here
[versions]
fabric-loom = "1.17.16"
minivan = "0.5"

[plugins]
fabric-loom = { id = "net.fabricmc.fabric-loom-remap", version.ref = "fabric-loom" }
minivan = { id = "agency.highlysuspect.minivan", version.ref = "minivan" }
